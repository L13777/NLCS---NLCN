name: learning_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_slidable: ^3.1.0
  flutter_pdfview: ^1.3.2
  file_picker: ^8.0.1
  syncfusion_flutter_calendar: ^25.1.40

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  supertokens_flutter: ^0.4.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/images/UDBG.jpg
    - assets/images/userdefaultbg.jpg